machine:
  node:
    version: 6.0.0

test:
  post:
    - bash <(curl -s https://codecov.io/bash)

deployment:
  production:
    branch: master
    heroku:
      appname: webcom-server
  staging:
    branch: master
    heroku:
      appname: webcom-server-staging
